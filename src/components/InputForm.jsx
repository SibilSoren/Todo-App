import React from "react";

const InputForm = () => {
  return (
    <div>
      <input type="text" placeholder="Enter your todo's" />
      <button>Add</button>
    </div>
  );
};

export default InputForm;
